# Этап 2: Детальное техническое задание - Стриминговая обработка

## 📋 Обзор

Необходимо добавить streaming обработку данных через Apache Kafka и Spark Structured Streaming. Streaming обработка должна работать параллельно с батчевой частью и записывать результаты в тот же MinIO bucket.

---

## 🏗️ Архитектура решения

```
┌─────────────┐
│   Producer  │ → Generates events in real-time
└──────┬──────┘
       │
       ▼
┌─────────────┐
│   Kafka     │
│  (Broker)   │
└──────┬──────┘
       │
       ▼
┌─────────────────┐
│ Spark Structured│
│   Streaming     │
│                 │
│  - Read         │
│  - Aggregate    │
│  - Transform    │
└──────┬──────────┘
       │
       ▼
┌─────────────────┐
│   Storage       │
│  (S3-compatible)│
│                 │
│  Parquet files  │
└─────────────────┘

┌─────────────────┐
│   Monitoring    │
│   (Prometheus   │
│   + Grafana)    │
└─────────────────┘
```

---

## 1️⃣ Настройка Kafka

### 1.1 Добавить Kafka в docker-compose.yml

Добавьте Kafka в docker-compose.yml.

---

## 2️⃣ Kafka Producer

### 2.1 Структура проекта

Определите структуру проекта самостоятельно.

### 2.2 Реализация producer.py

Реализуйте Kafka producer.

### 2.3 requirements.txt

Определите зависимости самостоятельно.

### 2.4 Dockerfile

Создайте Dockerfile для producer.

### 2.5 Добавить в docker-compose.yml

Добавьте producer сервис в docker-compose.yml.

---

## 3️⃣ Spark Structured Streaming Job

### 3.1 Реализация streaming_job.py

Реализуйте Spark Structured Streaming job:
- Читать из Kafka
- Парсить JSON
- Обработать данные
- Реализовать windowed aggregations
- Настроить watermarking
- Настроить checkpointing
- Записать результаты в Parquet

### 3.2 Добавить в docker-compose.yml

Добавьте Spark streaming job в docker-compose.yml.

---

## 4️⃣ Мониторинг (Prometheus + Grafana)

### 4.1 Настройка мониторинга

Настройте мониторинг (Prometheus + Grafana).

### 4.2 Grafana Dashboard

Создайте dashboard в Grafana для визуализации метрик.

---

## 5️⃣ Интеграция с Batch

### 5.1 Объединённый запрос

Реализуйте возможность объединения batch и streaming данных для запросов.

---

## ✅ Чеклист выполнения

- [ ] Kafka запускается (Zookeeper + Kafka broker)
- [ ] Kafka topic создан
- [ ] Producer отправляет данные в Kafka
- [ ] Spark Structured Streaming читает из Kafka
- [ ] JSON парсится корректно
- [ ] Фильтрация работает
- [ ] Windowed aggregations работают
- [ ] Watermarking настроен
- [ ] Checkpointing работает (job восстанавливается после рестарта)
- [ ] Данные записываются в MinIO в Parquet
- [ ] Мониторинг настроен (Prometheus + Grafana)
- [ ] Grafana dashboard показывает метрики
- [ ] Streaming и batch данные можно объединить

---

