# Генерация данных - Windows (PowerShell)

## 📋 Обзор

Генератор данных создаёт большие JSONL файлы с реалистичными проблемами качества данных для тестирования вашего пайплайна.

## 🚀 Быстрый старт

### Для разработки (рекомендуется)

```powershell
# Маленькие файлы (1GB каждый, 2 файла) - генерируется за 1-2 минуты
.\generate-data.ps1 -Small
```

### Для финального решения

```powershell
# Большие файлы (16GB каждый, 2 файла) - генерируется за 15-30 минут
.\generate-data.ps1
```

### Кастомный размер

```powershell
# Генерировать 3 файла по 20GB каждый
.\generate-data.ps1 -Size 20 -Count 3
```

## 📊 Проверка результата

```powershell
# Статистика файлов (размеры, количество строк)
.\check-data.ps1

# Валидация структуры (проверка JSON)
.\validate-data.ps1

# Просмотр файлов
Get-ChildItem data\ | Format-Table Name, @{Label="Size (GB)"; Expression={[math]::Round($_.Length/1GB, 2)}}
```

## 📁 Что будет сгенерировано

В папке `data/` появятся:

1. **Большие файлы** (JSONL):
   - `events_large_1.jsonl` - минимум 16GB (или заданный размер)
   - `events_large_2.jsonl` - минимум 16GB (или заданный размер)
   - И другие если указано `-Count > 2`

2. **Справочные файлы** (CSV, маленькие <1MB):
   - `device_info.csv` - ~100 строк
   - `firmware_info.csv` - ~50 строк

## ⚙️ Параметры генерации

### Параметры скрипта

```powershell
.\generate-data.ps1 [-Size <GB>] [-Count <number>] [-Small]

Параметры:
  -Size <GB>      Размер каждого файла в GB (по умолчанию 16)
  -Count <number> Количество больших файлов (по умолчанию 2)
  -Small          Быстрый режим (1GB файлы, 2 штуки)
```

### Параметры через переменные окружения

Если нужен более детальный контроль, можно задать через переменные окружения:

```powershell
# Установить переменные окружения
$env:DATA_FILE_SIZE_GB = 16
$env:NUM_LARGE_FILES = 2
$env:CORRUPTION_RATE = 0.001          # 0.1% порченных записей
$env:MISSING_DATA_RATE = 0.05         # 5% записей с пропусками
$env:DUPLICATE_RATE = 0.02            # 2% дубликатов
$env:OUTLIER_RATE = 0.01              # 1% outliers

# Запустить генератор
.\generate-data.ps1
```

## 🔍 Характеристики данных

### Качество данных

Генератор создаёт данные с реалистичными проблемами качества данных. Изучите сгенерированные файлы самостоятельно и определите какие проблемы присутствуют в данных.

### Схема данных

```json
{
  "event_id": 123456,
  "device_id": "dev_1",
  "device_type": "sensor_A",
  "event_time": "2024-01-15T10:30:45.123456+00:00",
  "event_duration": 2.345,
  "location": {
    "latitude": 40.712776,
    "longitude": -74.005974,
    "city": "New York",
    "country": "United States"
  },
  "metadata": {
    "firmware_version": "3.2.15",
    "battery_level": 85,
    "signal_strength": -75
  }
}
```

## ⏱️ Время генерации

- **1GB файл**: ~5-10 минут
- **16GB файл**: ~60-100 минут (зависит от скорости диска)
- **32GB файл**: ~120-200 минут

**💡 Совет:** Для разработки используйте `.\generate-data.ps1 -Small` (1GB файлы). Это позволит быстро тестировать код без долгого ожидания генерации.

## 🔧 Альтернатива: Локальный запуск (без Docker)

Если Docker недоступен или хотите запустить быстрее:

```powershell
# 1. Установить зависимости
cd data_generator
pip install -r requirements.txt

# 2. Запустить генератор локально
$env:DATA_FILE_SIZE_GB = 1
$env:NUM_LARGE_FILES = 2
$env:DATA_OUTPUT_DIR = "..\data"
python generate_data.py

# Или одной командой
cd data_generator
python generate_data.py
```

Скрипт `generate-data.ps1` автоматически использует локальный Python если Docker недоступен.

## 🧹 Очистка

```powershell
# Удалить сгенерированные файлы
.\clean-data.ps1

# Удалить Docker volumes (если использовали Docker)
docker-compose down -v
```

## ❓ Troubleshooting

### Ошибка: "Не удаётся загрузить файл... из-за выполнения скриптов отключено"

Разрешите выполнение скриптов:

```powershell
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### Ошибка: "Docker не найден"

Установите [Docker Desktop для Windows](https://www.docker.com/products/docker-desktop), или скрипт автоматически переключится на локальную генерацию через Python.

### Ошибка: "Python не найден"

Установите Python 3.11+ с [python.org](https://www.python.org/downloads/) или через Microsoft Store.

### Генерация идёт очень медленно

- Используйте `.\generate-data.ps1 -Small` для разработки (1GB файлы)
- Или запускайте локально через Python (автоматически если Docker недоступен)

### Нет места на диске

Проверьте свободное место:

```powershell
Get-PSDrive C | Select-Object Used,Free
```

Для 16GB файлов нужно минимум **35GB свободного места**.

### Файлы не появляются в папке data/

Проверьте:
1. Что скрипт завершился без ошибок
2. Что папка `data/` существует
3. Что достаточно места на диске

Если использовали Docker, проверьте Docker volumes:

```powershell
docker volume ls | Select-String "data"
```

---

**Успешной генерации! 🚀**
